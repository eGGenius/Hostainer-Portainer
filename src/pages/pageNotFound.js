import React from "react";
import { Container } from "react-bootstrap";

export default function PageNotFound() {
    return (
        <Container className="site-content">
            <p><b>Die Seite konnte nicht gefunden werden...</b></p>
        </Container>
    );
}

